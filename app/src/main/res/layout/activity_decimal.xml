<?xml version="1.0" encoding="utf-8"?>
<android.support.v4.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/decimal_activity_drawer_layout"
    style="@style/mm"
    tools:context="com.example.android.bits20.DecimalActivity">

    <ScrollView
        style="@style/mm"
        android:background="@drawable/background_gradient">

        <LinearLayout style="@style/mmv">

            <android.support.v7.widget.Toolbar
                android:id="@+id/decimal_activity_toolbar"
                android:layout_width="match_parent"
                android:layout_height="?actionBarSize"
                android:background="@color/colorPrimary"
                android:theme="@style/AppbarTheme"
                app:titleTextColor="@color/white"></android.support.v7.widget.Toolbar>

            <TextView
                style="@style/mw"
                android:text="Decimal Number system"
                android:textSize="35sp" />
        </LinearLayout>
    </ScrollView>

    <android.support.design.widget.NavigationView
        android:id="@+id/decimal_activity_navigation_view"
        style="@style/wm"
        android:layout_gravity="start"
        android:background="@drawable/background_gradient"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/third_activity_drawer_header"
        app:menu="@menu/introduction_drawer_menu" />

</android.support.v4.widget.DrawerLayout>
